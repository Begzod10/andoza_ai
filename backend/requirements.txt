fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic[email]==2.13.5
pydantic-settings==2.15.0
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1
redis[hiredis]==5.0.4
celery[redis]==5.4.0
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.32
httpx==0.27.0
reportlab==4.2.2
boto3==1.34.120
sentry-sdk[fastapi]==2.4.0
structlog==24.2.0
python-dotenv==1.0.1
openai>=1.0.0
# USDZ -> GLB room-scan conversion (replaces Blender: the prod VPS CPU has no SSE4.2).
# usd-core's manylinux x86-64 .so files disassemble with zero SSE4.2/AVX opcodes;
# numpy carries AVX kernels but selects them by runtime CPU dispatch off an SSE2
# baseline. Verified by running the whole conversion under `qemu-x86_64 -cpu qemu64`
# (no SSE4.2, same CPU model the VPS reports) -- see the converter's module docstring.
#
# The marker skips linux-aarch64 ONLY: usd-core publishes no linux-arm64 wheel, so
# an Apple-Silicon dev box building the linux/arm64 image would otherwise fail. Prod
# is x86_64 (wheel installs), and native macOS dev gets the universal2 wheel. Where
# it is absent, room_scan_glb.usdz_to_glb() just logs and returns False.
usd-core==25.11; platform_machine != "aarch64"
trimesh==5.1.0
numpy==2.2.6
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.1.0
fakeredis>=2.23
respx>=0.21.0
aiosqlite>=0.19.0
